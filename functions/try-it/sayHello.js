let makeSandwich = [breadType, fillingType, condiments];
for (let i = 0; i < makeSandwich; i++) {
   console.log(makeSandwich[i]);
}
function makeSandwich(breadType, fillingType, condiments) {
   for ()
      
}

